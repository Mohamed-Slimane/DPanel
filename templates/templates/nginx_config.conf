server {
    listen 80;
    listen [::]:80;

    server_name {{domain.name}};
    root {{domain.www_path}};

    location / {
        {% if domain.domain_app and domain.domain_app.is_active %}
        include uwsgi_params;
        uwsgi_pass 0.0.0.0:{{domain.domain_app.port}};
        {% endif %}
        index index.html;
        try_files $uri $uri/ =404;
    }

    error_page 404 /404.html;

    location ~* \.(jpg|jpeg|gif|css|png|js|ico|html|svg|webp|woff|woff2|ttf|eot|mp4|webm|ogg|mp3|txt|xml|json)$ {
        access_log off;
        expires max;
    }

    location ~ /\.(ht|git|svn|db|bak|env) {
        deny all;
    }

    location /. {
        return 404;
    }
}